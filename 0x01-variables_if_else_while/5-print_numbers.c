#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Executable function
 *
 * Return Success 0 (Always)
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
		printf("%s\n");
	}

	return (0);
}
